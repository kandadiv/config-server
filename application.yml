logging:
  level:
    com.self.sprintboot.learning.apigateway: DEBUG

skip:
 security: N            

spring:  
  application:
    name: agreements  
      # main:
  #  allow-bean-definition-overriding: true
  profiles:
    active: default
  cloud:
    config:
      uri: http://localhost:9296/
  security:
    oauth2:
      resourceserver:
        jwt:         
         issuer-uri: https://cognito-idp.us-east-1.amazonaws.com/us-east-1_zgkHvtA9m 


management:
  security:
    enabled: false

service:
  controller:
    doc: This controller used for finding agreement information with varios GET operations.
  searhcagreements:
    doc: Search agreements by agreement id , agreement number , social security or  first name and  last name with status, type , line of bueinss parametersn. 

